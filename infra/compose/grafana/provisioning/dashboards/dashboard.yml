apiVersion: 1

providers:
  - name: multimodal-api
    orgId: 1
    folder: Multimodal API
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
